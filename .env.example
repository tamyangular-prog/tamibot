# ============================================================
# 🤖 Bot Lu - Arquivo de Configuração (EXEMPLO)
# ============================================================
# 1. Copie este arquivo para .env
# 2. Configure as variáveis abaixo
# 3. NUNCA commit .env em Git (apenas .env.example)

# Ativar/desativar o controlador HTTP (painel web)
# true  = Painel web ativo em /api/bot/*
# false = Apenas bot WhatsApp (sem painel)
ENABLE_HTTP_CONTROLLER=true

# Porta do servidor Express
# Localhost: use 3000
# Hostinger: mantenha como 3000 (sistema atribui automaticamente)
PORT=3000

# 🔐 SEGURANÇA: Token de autenticação para API
# ⚠️  MUDE ISTO ANTES DE USAR EM PRODUÇÃO!
# Use um token único e seguro (mínimo 20 caracteres)
# Exemplo seguro: xyz123abc456uvw789def012ghi345
API_TOKEN=seu-token-super-secreto-aqui

# Ambiente de execução
# development = Mais logs, sem validações rígidas
# production  = Menos logs, validações rigorosas
NODE_ENV=production

# ============================================================
# PRÓXIMAS ETAPAS
# ============================================================
# Para Hostinger:
# 1. Crie um arquivo .env na pasta /public_html/bot-hostinger/
# 2. Copie as variáveis deste arquivo
# 3. MUDE o API_TOKEN para algo único
# 4. Mantenha NODE_ENV=production
